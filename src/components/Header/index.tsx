import React from 'react';
import * as S from './styled';

const Header = () => {
    return (
        <S.Header>
            <S.Title>Menu</S.Title>
        </S.Header>
    );
};

export default Header;
